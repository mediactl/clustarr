---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: subtitleproviders.subtitle.clustarr.io
spec:
  group: subtitle.clustarr.io
  names:
    categories:
    - clustarr
    kind: SubtitleProvider
    listKind: SubtitleProviderList
    plural: subtitleproviders
    singular: subtitleprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Throttled")].status
      name: Throttled
      priority: 1
      type: string
    - jsonPath: .status.quota.remaining
      name: Quota
      priority: 1
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SubtitleProvider is one upstream subtitle source, its credentials and its
          current rate-limit state.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubtitleProviderSpec defines the desired state of SubtitleProvider.
            properties:
              enabled:
                default: true
                description: Enabled allows searches against this provider.
                type: boolean
              endpoint:
                description: Endpoint overrides the provider base URL, for mirrors
                  or local instances.
                maxLength: 2048
                type: string
              languages:
                description: |-
                  Languages restricts this provider to the given BCP-47 language tags.
                  Empty means the provider is searched for every wanted language.
                items:
                  maxLength: 32
                  type: string
                maxItems: 64
                type: array
                x-kubernetes-list-type: set
              options:
                additionalProperties:
                  type: string
                description: |-
                  Options holds non-secret provider settings such as aiTranslated=exclude,
                  trustedSources=true or vip=true.
                type: object
              priority:
                default: 50
                description: |-
                  Priority orders providers when a profile does not list them explicitly;
                  lower is searched first.
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              requestsPerSecondMilli:
                default: 5000
                description: |-
                  RequestsPerSecondMilli is the client-side rate limit in thousandths of a
                  request per second, so 5 requests per second is 5000. A scaled integer is
                  used because CRD schemas do not allow floating-point values.
                format: int32
                minimum: 1
                type: integer
              secretRef:
                description: |-
                  SecretRef names a Secret in the same namespace holding the provider
                  credentials. Recognised keys: apiKey, username, password. Credentials are
                  never inlined in the spec.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              type:
                description: |-
                  Type is the upstream subtitle source. It is immutable: point a new
                  SubtitleProvider at a different source instead.
                enum:
                - opensubtitlescom
                - gestdown
                - subdl
                - subsource
                - embedded
                - whisper
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - type
            type: object
          status:
            description: SubtitleProviderStatus defines the observed state of SubtitleProvider.
            properties:
              conditions:
                description: Conditions holds Ready, Authenticated and Throttled.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              errorsLast120s:
                description: |-
                  ErrorsLast120s is the number of failed requests in the last two minutes,
                  used to decide when to throttle.
                format: int32
                minimum: 0
                type: integer
              hiVerifiable:
                description: |-
                  HIVerifiable is true when this provider reports a trustworthy
                  hearing-impaired flag, so HI policies can be enforced against it.
                type: boolean
              lastSuccessAt:
                description: LastSuccessAt is when a request to this provider last
                  succeeded.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              quota:
                description: Quota is the download allowance the provider last reported.
                properties:
                  remaining:
                    description: Remaining is the number of downloads left in the
                      current quota window.
                    format: int32
                    minimum: 0
                    type: integer
                  resetAt:
                    description: ResetAt is when the quota window rolls over.
                    format: date-time
                    type: string
                type: object
              throttleReason:
                description: ThrottleReason explains the current throttle.
                maxLength: 512
                type: string
              throttledUntil:
                description: |-
                  ThrottledUntil is when the provider may be searched again. Unset means
                  the provider is not throttled.
                format: date-time
                type: string
              tokenExpiresAt:
                description: TokenExpiresAt is when the cached login token stops being
                  valid.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
