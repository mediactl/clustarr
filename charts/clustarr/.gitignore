# `helm dependency build` materialises the nats / cloudnative-pg / keda
# subcharts here from Chart.lock. They are build output, not source;
# Chart.lock is what is committed and what pins the versions.
charts/
*.tgz
