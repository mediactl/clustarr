# `helm dependency build` materialises the nats / nack / keda subcharts here
# from Chart.lock. They are build output, not source; Chart.lock is what is
# committed and what pins the versions.
charts/
*.tgz
